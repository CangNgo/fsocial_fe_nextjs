export enum Role {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum PostStatus {
  ACTIVE = "ACTIVE",
  HIDDEN = "HIDDEN",
  DELETED = "DELETED",
}

export enum ReportStatus {
  PENDING = "PENDING",
  RESOLVED = "RESOLVED",
  REJECTED = "REJECTED",
}
